language: rust
rust:
  - 1.31.1 # MSRV
  - nightly
  - beta
  - stable
jobs:
  allow_failures:
    - rust: nightly
script:
  - cargo build --verbose
  - cargo test --all --all-features -- --nocapture
